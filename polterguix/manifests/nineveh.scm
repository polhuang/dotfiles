;; no longer a manifest - converting soon hopefully

                   pinentry                     ;; security
                   emacs-next-pgtk-xwidgets     ;; emacs
                   emacs-desktop-environment    ;; emacs
                   emacs-guix                   ;; emacs
                   emacs-jinx                   ;; emacs
                   firefox                      ;; web 
                   kitty                        ;; tooling
                   qutebrowser                  ;; web
                   rofi-wayland                 ;; desktop
                   font-google-noto             ;; fonts
                   font-google-noto-emoji       ;; fonts
                   font-google-noto-sans-cjk    ;; fonts
                   mu                           ;; emacs
                   gnupg-new                    ;; security
                   emacs-jinx                   ;; emacs   
                   btop                         ;; base
                   librewolf                    ;; web
                   waybar                       ;; desktop
                   swaynotificationcenter       ;; desktop
                   neovim                       ;; tooling
                   password-store               ;; security
                   obs                          ;; media
                   rust                         ;; code
                   rust-cargo                   ;; code
                   hyprpaper                    ;; desktop
                   hypridle                     ;; desktop
                   starship-bin))           
